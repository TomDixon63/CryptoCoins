package io.tomdixon.cryptocoins.backend.service;

import org.springframework.stereotype.Service;

@Service
public class MongoDbService {
	/*
	 * 
	 * private final MongoRepository repository;
	 * 
	 * public MongoDbService(MongoRepository repository) {
	 * 
	 * super(); this.repository = repository; }
	 * 
	 * public Flux<WatchlistEntry> findAll() {
	 * 
	 * return repository.findAll();
	 * 
	 * }
	 * 
	 * public Mono<WatchlistEntry> findById(String id) {
	 * 
	 * return repository.findById(id);
	 * 
	 * }
	 * 
	 * public Mono<WatchlistEntry> update(WatchlistEntry w) {
	 * 
	 * return repository.save(w);
	 * 
	 * }
	 * 
	 * public Mono<Void> delete(String id) {
	 * 
	 * return repository.deleteById(id); }
	 * 
	 */
}
